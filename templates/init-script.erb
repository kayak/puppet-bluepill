#!/bin/bash
# Added by the bluepill module for Puppet
#   https://github.com/kayakco/puppet-bluepill

<% raise "@appname cannot be nil!" if @appname.nil? -%>

bluepill <%= @appname %> status %>/dev/null || ( bluepill load <%= @conffile %>; sleep 1 )

bluepill <%= @appname %> "$@"
